# 钢板表面缺陷分割项目思路

## 1. 项目核心判断

这个数据集本质上是钢板表面缺陷的像素级分割任务，而不是普通目标检测任务。

原始标注使用 RLE 编码表示缺陷区域，主要评价指标是 Dice。当前项目虽然使用了 `yolov8s-seg`，但数据转换、增强和评估流程没有完全对齐，因此当前效果不能简单归因于 YOLO 模型本身不适合。

当前最值得优先解决的问题是：

1. 确认 RLE 标注是否被正确转换为分割标签。
2. 确认图像变换时多边形标签是否同步变换。
3. 使用 Dice 作为主要评价指标，而不是只看 mAP。
4. 处理 4 个缺陷类别之间严重的数据不平衡。
5. 针对细长、小面积缺陷保留足够的图像细节。

---

## 2. 数据集概况

从 `data/train.csv` 统计得到：

- 原始图像数量：6666
- 标注实例总数：7095
- 缺陷类别数：4
- 原始图像尺寸约为：`1600 x 256`

类别分布如下：

| 类别 | 实例数 | 说明 |
|---|---:|---|
| defect_1 | 897 | 中等数量 |
| defect_2 | 247 | 少数类别，最容易被忽略 |
| defect_3 | 5150 | 占比最高，模型容易偏向此类 |
| defect_4 | 801 | 中等数量 |

当前训练集和验证集还包含大量空标签图像：

- 训练集：10054 张，其中 4706 张为空标签
- 验证集：2514 张，其中 1204 张为空标签

需要确认这些空标签图像是否确实应该作为负样本。如果它们是数据处理或增强过程中产生的错误样本，可能会显著影响训练和评估。

---

## 3. 当前代码中需要重点检查的问题

### 3.1 RLE 转换脚本可能丢失分割信息

`scripts/convert_to_yolo.py` 使用 `cv2.boundingRect` 生成的是矩形框：

```python
x, y, w, h = cv2.boundingRect(cnt)
f.write(f"{class_id} {cx:.6f} {cy:.6f} {w_norm:.6f} {h_norm:.6f}\\n")
```

这种格式是目标检测框格式，不是 YOLO 分割多边形格式。

YOLO 分割标签应该类似：

```text
class_id x1 y1 x2 y2 x3 y3 ...
```

因此需要确认 `steel_dataset/train/labels` 和 `steel_dataset/val/labels` 最终使用的是哪个转换脚本。

### 3.2 `convert_to_yolo2.py` 存在方向和尺寸风险

该脚本包含：

```python
mask_pil = mask_pil.rotate(90, expand=True)
mask_pil = ImageOps.flip(mask_pil)
```

同时把图像尺寸写死为：

```python
normalized_x = round(x / 1600, 6)
normalized_y = round(y / 256, 6)
```

这可能造成以下问题：

- mask 方向与原图不一致；
- 旋转后标签和图像尺寸不一致；
- 多边形整体偏移；
- Dice 明显下降。

更稳妥的方式是直接按照原始图像尺寸解码 RLE，并在原图坐标系中提取轮廓，不再额外旋转或翻转。

### 3.3 自定义增强中的标签没有始终同步

`scripts/train_improved.py` 中水平翻转和平移更新了标签，但缩放和旋转没有同步更新多边形坐标。

例如：

```python
resized = cv2.resize(out, (int(w * scale), int(h * scale)))
```

以及：

```python
M = cv2.getRotationMatrix2D((w / 2, h / 2), angle, 1.0)
out = cv2.warpAffine(out, M, (w, h), borderMode=cv2.BORDER_REFLECT)
```

如果图像发生了缩放或旋转而标签没有同步变化，就会产生错误监督。

第一轮基线实验应该关闭这些自定义离线增强。后续增强应使用 Albumentations 或 Ultralytics 内置增强，保证图像和标签同步。

### 3.4 Copy-Paste 暂时不应作为第一优先级

当前 Copy-Paste 实现比较简单，可能产生：

- 不自然的缺陷纹理；
- 缺陷与背景融合不真实；
- 新缺陷与原有缺陷重叠；
- 标签越界或边界不一致。

在标签质量和基线结果确认以前，不建议继续增加 Copy-Paste 倍率。

---

## 4. 推荐的项目目标

不要把项目目标简单定义为“把 YOLO 的准确率调高”。更适合的小项目目标是：

> 研究不同数据处理方法对钢板表面缺陷像素级分割 Dice 的影响。

可选项目标题：

### 方向一：完整对比型

> 面向钢板表面缺陷的像素级分割方法研究：YOLO 实例分割与 U-Net 语义分割的对比

### 方向二：更小、更容易完成

> 钢板细长缺陷分割中的标签转换、高分辨率输入与类别不平衡研究

### 方向三：偏工程实践

> 基于深度学习的钢板表面缺陷自动分割系统

---

## 5. 推荐实验路线

### 实验 A：干净数据基线

目标：确认数据和标签本身没有问题。

设置：

- 直接从原始 RLE 生成 mask 或正确的多边形标签；
- 不使用离线增强；
- 不使用 Copy-Paste；
- 使用 `YOLOv8s-seg`；
- `imgsz=1024` 或 `1280`；
- 训练 50 到 100 个 epoch；
- 验证集保持固定。

这是所有后续实验的基础。

### 实验 B：只处理类别不平衡

只增加 `defect_2` 的训练样本，建议分阶段比较：

```text
原始比例
 defect_2 过采样 2 倍
 defect_2 过采样 4 倍
```

不要一开始就对少数类别进行 8 倍甚至更高倍率过采样，也不要同时加入很多增强策略，否则无法判断究竟是哪一项产生了影响。

### 实验 C：提高输入分辨率

原始图片大约为 `1600 x 256`，缺陷通常细长且面积小。直接缩放到正方形可能损失细节。

可以比较：

- `imgsz=1024`
- `imgsz=1280`
- `imgsz=1600`
- 保持长宽比的训练方式
- 将图片按宽度切成 2 到 4 个 tile，再进行训练和推理

对于细长缺陷，高分辨率或切片通常比简单替换模型规模更值得优先尝试。

### 实验 D：语义分割模型对比

如果任务只关心每个类别的缺陷区域，而不要求区分同一类别中的不同实例，可以使用语义分割模型进行对比：

- U-Net
- U-Net++
- DeepLabV3+
- SegFormer

比较：

1. 四类语义分割；
2. YOLO 实例分割；
3. 每一类的 Dice；
4. 推理速度；
5. 缺陷边界可视化效果。

由于原始任务主要按像素 Dice 评分，语义分割模型可能比实例分割模型更贴近任务目标。

---

## 6. 评估指标设计

不要只看 YOLO 默认输出的 mAP。建议每次实验至少记录：

- defect_1 Dice
- defect_2 Dice
- defect_3 Dice
- defect_4 Dice
- 所有图像平均 Dice
- 仅包含缺陷图像的平均 Dice
- 每类 precision
- 每类 recall
- 推理速度

建议同时报告两类 Dice：

```text
all_image_dice
 defect_image_dice
```

如果大量空图像直接按 Dice=1 计算，整体平均值可能会被空图拉高，无法真实反映模型在缺陷区域上的表现。

评估时还应检查：

- 预测 mask 是否已经还原到原始图像尺寸；
- letterbox padding 是否被正确移除；
- ground truth 是否来自原始 RLE；
- 同一类别的多个预测 mask 是否正确合并。

---

## 7. 最建议的实际执行顺序

### 第一步：建立干净数据集

建议新建：

```text
steel_dataset_clean/
    train/images/
    train/labels/
    val/images/
    val/labels/
    data.yaml
```

直接使用原始 RLE 生成标签，不使用矩形框转换，也暂时不使用有旋转风险的脚本。

### 第二步：可视化验证标签

随机选择至少 50 张图像，显示：

- 原图；
- RLE 解码得到的 mask；
- 转换后的 YOLO 多边形；
- 多边形重新生成的 mask。

只有当转换后的多边形和原始 mask 基本重合后，才开始训练模型。

### 第三步：训练无增强基线

建议初始配置：

```python
model = YOLO("models/yolov8s-seg.pt")

model.train(
    data="steel_dataset_clean/data.yaml",
    epochs=80,
    imgsz=1280,
    batch=16,
    device=0,
    pretrained=True,
    mosaic=0.0,
    mixup=0.0,
    copy_paste=0.0,
    patience=20,
)
```

### 第四步：一次只改变一个因素

推荐实验表：

| 实验 | 模型 | 增强 | 输入尺寸 | defect_1 | defect_2 | defect_3 | defect_4 | 平均 Dice |
|---|---|---|---:|---:|---:|---:|---:|---:|
| A | YOLOv8s-seg | 无 | 1024 | | | | | |
| B | YOLOv8s-seg | defect_2 过采样 | 1024 | | | | | |
| C | YOLOv8s-seg | 无 | 1280 | | | | | |
| D | U-Net 或 SegFormer | 无 | 原尺寸 | | | | | |

---

## 8. 对 YOLO 的最终判断

暂时不需要放弃 YOLO。

YOLOv8-seg 适合作为实例分割基线，优点是训练和部署简单，也能同时预测类别和缺陷区域。但它不一定是这个像素 Dice 任务的最终最佳模型。

建议采用以下定位：

1. YOLOv8-seg：实例分割基线；
2. U-Net 或 SegFormer：像素级语义分割基线；
3. 用同一验证集和同一 Dice 计算方式进行比较；
4. 根据 Dice、边界质量和推理速度选择最终模型。

当前训练结果中整体分割 mAP 约为 0.56，最弱类别约为 0.20，说明模型并非完全没有学习，而是受到少数类别、标签质量、分辨率和评估方式的共同影响。

最重要的下一步不是继续堆训练轮数或增强倍率，而是：

> 先验证 RLE 到 mask/多边形的转换正确，再建立无增强基线，最后逐项研究类别平衡和高分辨率输入。
